function renderCheckout(){const step=state.checkoutStep,q=totalQty(),quote=isQuote();let body='';if(step===0)body=`<div class="step-kicker">Paso 1 de 4</div><h2 class="step-title">¿Cómo lo quieres?</h2><p class="step-sub">Elige recolección, domicilio o logística para evento.</p><div class="choice-list">${fulfillmentOptions.map(o=>`<button class="choice-card ${state.fulfillment===o.id?'selected':''}" data-fulfillment="${o.id}"><span class="choice-icon">${icon(o.icon)}</span><span><b>${o.title}</b><small>${o.copy}</small></span><i class="radio"></i></button>`).join('')}</div>${state.fulfillment==='delivery'?`<div class="delivery-inline"><label class="field-label">Dirección, colonia o código postal</label><input class="shipping-input" id="addressField" placeholder="Ej. Del Valle, 03100" value="${escapeHtml(state.address)}" autocomplete="street-address">${shippingMarkup()}</div>`:''}`;if(step===1)body=`<div class="step-kicker">Paso 2 de 4</div><h2 class="step-title">¿Para qué plan?</h2><p class="step-sub">Esto nos ayuda a recomendar tiempos y logística.</p><div class="occasion-grid">${OCCASIONS.map(o=>`<button class="occasion-choice ${state.occasion===o.id?'selected':''}" data-checkoccasion="${o.id}"><b>${o.title}</b><small>${o.copy}</small></button>`).join('')}</div><div class="field-grid two"><div><label class="field-label">Fecha</label><input class="field" type="date" id="dateField" value="${state.date}"></div><div><label class="field-label">Hora aproximada</label><input class="field" type="time" id="timeField" value="${state.time}"></div></div>`;if(step===2)body=`<div class="step-kicker">Paso 3 de 4</div><h2 class="step-title">¿Cómo quieres las latas?</h2><p class="step-sub">Elige presentación normal o cuéntanos si necesitas personalización.</p><div class="personal-grid">${personalOptions.map(o=>`<button class="personal-card ${state.personalization===o.id?'selected':''}" data-personal="${o.id}"><span><b>${o.title}</b><small>${o.copy}</small></span><strong>${o.label}</strong></button>`).join('')}</div>${state.personalization!=='normal'?`<div style="margin-top:14px"><label class="field-label">¿Qué te gustaría poner?</label><textarea class="field textarea" id="personalText" placeholder="Nombre, frase, logo o concepto">${escapeHtml(state.personalizationText)}</textarea></div>`:''}${hasAlcohol()?`<div class="confirm-box"><input type="checkbox" id="adultConfirm" ${state.adultConfirmed?'checked':''}><label for="adultConfirm">Confirmo que las bebidas con alcohol son para personas adultas.</label></div>`:''}`;if(step===3){const f=fulfillmentOptions.find(x=>x.id===state.fulfillment)?.title||'—',o=OCCASIONS.find(x=>x.id===state.occasion)?.title||'—',p=pricePlan(q),tot=totalEstimate();body=`<div class="step-kicker">Paso 4 de 4</div><h2 class="step-title">¿A nombre de quién?</h2><p class="step-sub">Con esto armamos el mensaje final para WhatsApp.</p><div class="field-grid"><div><label class="field-label">Nombre</label><input class="field" id="nameField" placeholder="Tu nombre" value="${escapeHtml(state.name)}"></div><div><label class="field-label">WhatsApp</label><input class="field" id="phoneField" inputmode="tel" placeholder="+52 55 0000 0000" value="${escapeHtml(state.phone)}"></div><div><label class="field-label">Notas opcionales</label><textarea class="field textarea" id="notesField" placeholder="Algo que debamos considerar">${escapeHtml(state.notes)}</textarea></div></div><div class="review-summary"><h3>Resumen</h3><div class="review-line"><span>Selección</span><b>${q} bebidas · ${cartItems().length} sabores</b></div>${quote?`<div class="review-line"><span>Cantidad de referencia</span><b>${state.eventTarget||q} bebidas</b></div><div class="review-line"><span>Precio</span><b>Cotización por confirmar</b></div>`:`<div class="review-line"><span>Precio</span><b>$${p.unit} c/u · ${p.band}</b></div>`}<div class="review-line"><span>Plan</span><b>${o}</b></div><div class="review-line"><span>Entrega</span><b>${f}${state.shippingFee?` · ${money(state.shippingFee)}`:''}</b></div><div class="review-line"><span>Personalización</span><b>${personalOptions.find(x=>x.id===state.personalization)?.title}</b></div><div class="review-line"><span>${quote?'Estado':'Total estimado'}</span><b>${quote?'Por cotizar':money(tot)}</b></div></div>`}$('#checkoutContent').innerHTML=checkoutHeader()+body;$('#checkoutBack').onclick=()=>{saveFields();if(step===0)showScreen('cart');else{state.checkoutStep--;renderCheckout()}};$$('[data-fulfillment]').forEach(b=>b.onclick=()=>{saveFields();state.fulfillment=b.dataset.fulfillment;if(state.fulfillment!=='delivery'){state.shippingFee=0;state.shippingStatus=''}renderCheckout()});$$('[data-checkoccasion]').forEach(b=>b.onclick=()=>{state.occasion=b.dataset.checkoccasion;if(state.occasion==='event')state.mode='quote';renderCheckout()});$$('[data-personal]').forEach(b=>b.onclick=()=>{saveFields();state.personalization=b.dataset.personal;renderCheckout()});const address=$('#addressField');if(address){address.addEventListener('input',e=>scheduleShipping(e.target.value));requestAnimationFrame(()=>{address.focus();address.setSelectionRange(address.value.length,address.value.length)})}$('#checkoutAction').classList.add('show');$('#checkoutMeta').textContent=`Paso ${step+1} de 4`;$('#checkoutTotal').textContent=quote?'Cotización':money(totalEstimate());$('#checkoutNext').textContent=step===3?(quote?'Solicitar por WhatsApp':'Enviar pedido a WhatsApp'):'Continuar'}
function saveFields(){if($('#addressField'))state.address=$('#addressField').value.trim();if($('#dateField'))state.date=$('#dateField').value;if($('#timeField'))state.time=$('#timeField').value;if($('#personalText'))state.personalizationText=$('#personalText').value.trim();if($('#adultConfirm'))state.adultConfirmed=$('#adultConfirm').checked;if($('#nameField'))state.name=$('#nameField').value.trim();if($('#phoneField'))state.phone=$('#phoneField').value.trim();if($('#notesField'))state.notes=$('#notesField').value.trim()}
function validateStep(){saveFields();const q=totalQty();if(state.checkoutStep===0){if(!state.fulfillment)return showToast('Elige cómo quieres recibirlo.'),false;if(state.fulfillment==='delivery'&&q<10)return showToast('La entrega a domicilio inicia desde 10 bebidas.'),false;if(state.fulfillment==='delivery'&&state.address.length<4)return showToast('Escribe una dirección o código postal.'),false;if(state.fulfillment==='delivery'&&state.shippingStatus==='loading')return showToast('Espera un momento mientras calculamos el envío.'),false}if(state.checkoutStep===1){if(!state.occasion)return showToast('Cuéntanos para qué plan es.'),false;if(!state.date)return showToast('Elige la fecha del pedido.'),false}if(state.checkoutStep===2){if(state.personalization!=='normal'&&!state.personalizationText)return showToast('Cuéntanos qué quieres personalizar.'),false;if(hasAlcohol()&&!state.adultConfirmed)return showToast('Confirma que el pedido con alcohol es para personas adultas.'),false}if(state.checkoutStep===3){if(!state.name)return showToast('Escribe tu nombre.'),false;if(state.phone.replace(/\D/g,'').length<10)return showToast('Escribe un WhatsApp válido.'),false}return true}
function buildMessage(){const quote=isQuote(),items=cartItems().map(x=>`• ${x.qty} × ${x.name}`).join('\n'),f=fulfillmentOptions.find(x=>x.id===state.fulfillment)?.title||'',o=OCCASIONS.find(x=>x.id===state.occasion)?.title||'',pers=personalOptions.find(x=>x.id===state.personalization)?.title||'',p=pricePlan(totalQty()),total=totalEstimate();return`Hola ANTOJO. ${quote?'Quiero solicitar una cotización para un evento':'Quiero hacer este pedido'} 👋\n\nBEBIDAS\n${items}\n\nCantidad seleccionada: ${totalQty()}${quote?`\nCantidad de referencia: ${state.eventTarget||totalQty()}`:`\nPrecio por cantidad: $${p.unit} c/u · ${p.band}\nSubtotal estimado: ${money(totalQty()*(p.unit+personalizationSurcharge()))}`}\nPersonalización: ${pers}${state.personalizationText?` — ${state.personalizationText}`:''}${state.fulfillment==='delivery'?`\nEnvío: ${state.shippingFee?`${money(state.shippingFee)} estimado · ${state.shippingZone}`:'por confirmar'}`:''}${!quote&&total?`\nTotal estimado: ${money(total)}`:''}\n\nDETALLES\nPlan: ${o}\nFecha: ${state.date}${state.time?` · ${state.time}`:''}\nEntrega: ${f}${state.address?` · ${state.address}`:''}\nNombre: ${state.name}\nWhatsApp: ${state.phone}${state.notes?`\nNotas: ${state.notes}`:''}\n\n¿Me ayudan a confirmar disponibilidad y ${quote?'enviarme la cotización final':'total final'}?`}
function sendWhatsApp(){const url=`https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(buildMessage())}`;showScreen('success');setTimeout(()=>window.open(url,'_blank','noopener'),250)}
function openWhatsAppText(text){window.open(`https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(text)}`,'_blank','noopener')}
