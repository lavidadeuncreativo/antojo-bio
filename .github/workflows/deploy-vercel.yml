name: Deploy ANTOJO to Vercel (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Legacy deployment disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy automatic deployment disabled. Use the current release workflow only."
