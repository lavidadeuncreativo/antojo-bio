# Vercel deployment trigger

This branch contains the clean frontend already present in `main` plus a user-merge trigger. Merge this pull request from the GitHub UI so Vercel sees a commit associated with the repository owner's GitHub identity.
